#include <string.h>
#include <stdlib.h>
#include <stdio.h>

struct Produto{
    int id, estoque, validade;
    char nome[50];
    float preco;
};
int main(){

    struct Produto produtos[100];
    
    int encontrado=0,opcao,total=0,i,busca,j;
    char nomeBusca[50];

    do{
            printf("\n1-Cadastrar\n2-Listar\n3-Atualizar\n4-Remover\n5-Buscar\n0-Sair\n");
            scanf("%d",&opcao);

        //menu
        switch(opcao){
        
            case 1: //Cadastrar
        if(total>=100){ //Limite de Produtos
                printf("Limite de produtos Atingido!\n");
                break;
            }
            printf("ID:");
            scanf("%d",&busca);

            int repetido = 0; //Verificar ID repetido
            for(i=0;i<total;i++){
                if(produtos[i].id == busca){
                    repetido = 1;
                }
            }
        if(repetido == 1){
            printf("ID ja cadastrado!\n");
                break;
            }
            produtos[total].id=busca;
    
            printf("Nome:");
            scanf("%s",produtos[total].nome);
    
            printf("Preco:");
            scanf("%f",&produtos[total].preco);

            printf("Estoque:");
            scanf("%d",&produtos[total].estoque);

            printf("Validade em dias:");
            scanf("%d",&produtos[total].validade);

        total++;

            break;
            
            case 2: //Listar
        for(i=0;i<total;i++){

            printf("\nID: %d\n",produtos[i].id);
            printf("\nNome: %s\n",produtos[i].nome);
            printf("\nPreco: %.2f\n",produtos[i].preco);
            printf("\nEstoque: %d\n",produtos[i].estoque);
            printf("\nValidade em dias: %d dias\n", produtos[i].validade);

            //Alerta estoque baixo
        if(produtos[i].estoque < 5){
            printf("ALERTA:ESTOQUE BAIXO\n");
            }
            //Alerta de validade
        if(produtos[i].validade <= 30){
            printf("ALERTA:PRODUTO PERTO DA VALIDADE\n");
        }
        }
            break;

            case 3: //Atualizar
            printf("Digite o ID:");
            scanf("%d",&busca);
        
        for(i=0;i<total;i++){
        if(produtos[i].id == busca){
            printf("Novo estoque:");
            scanf("%d",&produtos[i].estoque);
            }
        }
            break;

            case 4: //Remover
            printf("Digite o ID:");
            scanf("%d",&busca);

            for(i=0;i<total;i++){
                if(produtos[i].id == busca){
            
            for(j=i;j<total-1;j++){
            
            produtos[j] = produtos[j+1];
        }

            total--;
            break;
            }
        }
            break;

            case 5: //Buscar
            printf("\n1-ID\n2-NOME\n");
            scanf("%d",&opcao);
    if(opcao==1){ //ID
            printf("Digite o ID:");
            scanf("%d",&busca);

            encontrado = 0;

            for(i=0;i<total;i++){
        if(produtos[i].id == busca){
            printf("\nProduto Encontrado\n");
            printf("ID: %d\n",produtos[i].id);
            printf("Nome: %s\n",produtos[i].nome);
            
            encontrado = 1;
                }
            }
        if (encontrado == 0){
            printf("Produto nao encontrado\n");
                }
        }
    else if(opcao == 2){ //NOME

            printf("Digite o Nome:");
            scanf("%s",nomeBusca);

            encontrado = 0;

            for(i=0;i<total;i++){

        if(strcmp(produtos[i].nome,nomeBusca) == 0){
            printf("\nProduto Encontrado\n");
            printf("ID: %d\n",produtos[i].id);
            printf("Nome: %s\n",produtos[i].nome);
            encontrado = 1;
            }
        }  
        if (encontrado == 0){
            printf("Produto nao encontrado\n");
        }
    }
        else {
            printf("Numero Invalido");
        }
            break;
        }

    } while(opcao != 0); 

    return 0;
}
